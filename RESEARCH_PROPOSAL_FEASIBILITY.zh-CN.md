# 可行性感知的跨具身学习 —— 研究提案（2026-08-06）

目标会议：机器人学顶级会议（CoRL / ICRA / RSS）。基础系统：我们在 UR10e 交接
（handover）平台上对 X-Diffusion 的 HRC 扩展（统一双向交接，已在机器人上验证）。
本文档回答 2026-08-06 提出的三个问题：

1. X-Diffusion 的判别器能否成为**部署时的安全层**？（详细设计）
2. 它能否将数据集**标注**为可行/不可行两部分用于训练？（设计 + 扩展）
3. X-Diffusion 框架中还有哪些地方存在**科学贡献**的空间？

---

## 0. 试点实验（2026-08-06）：判别器度量的是「身份」，不是「可行性」

在设计任何东西之前，我们先检验了核心前提。X-Diffusion 的分类器
`c_θ(k, A_t^k, s_t)` 是按具身身份（BCE 二分类，人类=0 / 机器人=1，论文 Eq. 2）
来*训练*的，但被*解读*为运动学/动力学可行性（k*，Eq. 3；Fig. 5「朴素联合训练学到
不可行动作」）。这两个概念只有在「不可行的动作恰好呈现出人类风格」时才会重合。
它们会不会出现分离？

**实验流程**（`scratchpad/shield_pilot.py`）：冻结的 `classifier_handover_uni`
（验证损失 0.514），使用 `handover_bowl_uni` 的验证窗口（347 段机器人、603 段
人类），对机器人验证片段在原始空间中施加 8 种物理动机的扰动族，在扩散步
k ∈ {0,5,10,20,40} 处打分，平均 8 次噪声采样。AUROC = 干净机器人 vs 扰动数据。

| 扰动族（施加于机器人片段上） | 违反的约束 | P(robot) @k0 | AUROC @k0 |
|---|---|---|---|
| 干净机器人数据 | — | 0.788 | — |
| 干净人类数据（重定向） | 仅身份 | 0.018 | **0.98** |
| 速度 ×2 / ×4 | 速度限制 | 0.785 / 0.778 | 0.50 / **0.51（随机水平）** |
| 片段中途瞬移 +5 cm | 连续性 | **0.861（比干净机器人还高！）** | 0.55 |
| 抖动 10 mm iid | 平滑性 | 0.782 | 0.52 |
| 甩鞭式抖动 ±40 mm（08-05 在机器人上实测到的真实 OOD 故障） | 动力学 | 0.818 | 0.51 |
| 夹爪 0/1 逐步闪烁 | 夹爪语义 | 0.692 | 0.67 |
| 欧拉角旋转 0.3 rad/step | 旋转动力学 | 0.528 | 0.77 |
| 静止（冻结） | 无违反（可行） | 0.785 | 0.50 ✓ 正确保留 |

**发现。**
- 身份区分近乎完美（0.98）—— 分类器很好地完成了它被训练的任务。
- 它**对动力学上的不可行性视而不见**（速度 ×4 处于随机水平；片段中途的瞬移得分
  甚至*比*干净机器人数据*更*像机器人）。根本原因：我们的人类数据在采集时刻意
  与机器人速度对齐，且两个类别在速度/连续性维度上都是连续变化的 —— 训练类别
  从未在这些轴上产生*差异*，分类器也就从未学到它们。在原论文的任务中，人类
  风格（侧向抓取）恰好与不可行性*相关*，所以身份可以作为可行性的代理；但这个
  代理恰恰在「不可行动作呈现机器人风格」时失效 —— 而这正是一个故障策略会输出
  的样子（我们实测的甩鞭式抖动突发在该分类器下有 98% 通过率）。
- 因此，朴素地「把判别器当作运行时安全阀」在**经验上是双重错误**的：它放行了
  危险输出（甩鞭式抖动仅 2% 被拒绝），又拒绝了良性的人类风格输出（同一阈值下
  88% 的干净人类片段被拒绝）。
- 在夹爪闪烁（0.67）和欧拉角旋转（0.77）上有微弱但真实的信号：它实际学到的是
  具身**风格**通道（抓取模式、旋转惯例）。

这就是这篇论文的科学切入点：**「具身身份 ≠ 执行可行性」**——对 X-Diffusion
核心机制的一次诊断，并附带下文的建设性修复方案。它也*印证了显式采集不可行数据
的必要性*：可行性信号无法从 H/R 分类器中免费提取出来，因为这个信号根本不在它
的训练类别里。

---

## 1. 设计 A —— 可行性判别器 `D_feas`（升级版判别器）

一个网络，架构与现有分类器相同（在带噪的 8×7 动作片段上运行的 HalfUnet1D，
以 7 维本体感受状态 + 扩散步 k 为条件，GAP + 线性头）→ 可直接替换现有分类器的
所有使用场景。新的训练类别如下：

**正例（可行）：**
- 所有机器人示范（平台上的任何任务）；
- 可选：通过下文解析式筛查的重定向人类示范，标签平滑至 0.9。

**负例（不可行），三个互补来源：**

1. **合成动力学扰动**（免费、无限量）：在数据加载器中实时施加试点实验里的 8 种
   扰动族，幅度随机化（速度 ∈ [1.5,6]×，瞬移 ∈ [2,10] cm，抖动 ∈ [5,25] mm，
   甩鞭式抖动 ∈ [15,60] mm，欧拉角 ∈ [0.1,0.5] rad/step，夹爪闪烁）。覆盖
   *运动学/动力学*层面的不可行性。课程学习：先大幅度，逐步退火到决策边界附近。
2. **解析式自动标注**（免费）：将重定向后的人类末端执行器（EEF）轨迹回放通过
   UR10e 差分逆运动学 + 限位检查（关节速度/加速度、工作空间范围、自碰撞代理
   检测）。违反限制的窗口 → 标为负例。这把论文（§V-B，丢弃 50%）中 FILTERED
   基线的人工筛查步骤，变成了一个自动的*标注器*而不是*数据丢弃器*。
3. **采集得到的可供性负例**（新的两份人类数据集，见 §4）：人手能做但 2F-85
   夹爪做不到的动作 —— 这些动作重定向后得到的是*在限位内*但仍不可执行的 EEF
   轨迹（手内重定向 → 夹爪闪烁 + 欧拉角噪声的特征信号）。这是任何解析式检查都
   无法捕捉的类别，而试点实验表明它是可学习的（闪烁/旋转正是已经出现信号的
   那两个扰动族）。

**输出头。** 共享主干的多任务头：`[identity, kin_feas, afford_feas]`
（3 个 sigmoid）。保留原有的身份判别功能（X-Diffusion 训练准入仍需要它），同时
新增经过校准的可行性输出；「单一可行性头 vs 多头」的消融实验可作为论文中的一张
表。噪声条件化机制被自动继承 → 每个头都定义在 k 轴上，保留了框架的核心思想
（可行性是*抽象层级的函数*），但现在度量的是可行性而不是身份。

文件：`models/xdiffusion/hr_classifier.py`（新增输出头维度 + 损失函数）、新建
`dataset_utils/corruptions.py`（扰动族、幅度、课程学习）、
`scripts/train_classifier.py`（配置中的负例来源混合权重）、
`scripts/ik_screen.py`（解析式标注器 → 逐窗口可行性 h5 键）。
预估工作量：2-3 天（含重新训练；分类器在我们的数据上约 30 分钟可训完）。

---

## 2. 设计 B —— 部署时安全阀（问题 1，详细实现）

### 2.1 集成位置

我们的部署已经采用了**批量采样 worker**流水线（`deploy_xdiffusion_ur10e.py`）：
父进程发送 `(state, n_draws)`，worker 返回 `(n, 8, 7)` 的动作片段批次。安全阀
就放在 worker 里（GPU 已经是热的）：

```
worker: chunks = policy.sample(state, n)                  # 已有逻辑
        f      = D_feas(chunks_normalized, state7, k=0)   # 新增：一次批量前向，
                                                          #      得到 (n,) 可行性分数
        return chunks, f                                  # 协议新增一个数组
```

成本：对 n≤4 个片段做一次 HalfUnet1D 前向 ≈ 1-2 ms —— 相较于 0.2 s 的扩散采样
可忽略不计；流水线化的周期预算不受影响。

### 2.2 决策策略（父进程，逐周期）

三个可组合的层级，通过 `--shield-mode {off,select,veto,both}` 标志控制：

1. **SELECT（软过滤）**：在 n 次采样中，丢弃 `f < τ_soft` 的样本，然后对幸存者
   应用现有的 `--draw-consistency` 锚点最近邻规则。当所有采样都通过时，退化为
   当前行为。如果 D_feas 能对 08-05 的甩鞭式抖动突发产生响应，仅此一项就足以
   过滤掉它们（试点实验表明：当前分类器*不能*识别 → 需要设计 A；用合成负例
   训练后，甩鞭式抖动会因构造而成为分布内的负例）。
2. **VETO（硬否决）**：如果最近 N 个周期中有 M 个周期（默认 3/4，带滞后以防
   抖动）的所有采样都满足 `f < τ_hard` → **不推进锚点**：保持当前姿态（重新
   下发锚点指令）、锁存夹爪状态、记录 `SHIELD-VETO` 日志。恢复条件：连续 K 个
   周期（默认 4）通过。这就是最初想法中「预测动作不可行时机器人什么都不做」，
   现在被精确定义为：*保持最后下发的姿态*，而不是零力矩（顺应控制器仍保持
   重力补偿；k_pos=400 用于抵抗漂移）。
3. **ESCALATE（升级处置）**：持续否决超过 T 秒（默认 6 s）→ 缓慢撤回到起始
   姿态，并要求操作员按键确认。与现有的稳定联锁和抓取锁存机制组合（安全阀
   在*持物*状态下否决时绝不能打开夹爪 —— 明确规则：否决同时冻结抓取通道）。

阈值校准是经验性且诚实的：设置 τ 使得来自我们*机器人已验证成功*部署会话（我们
留有相应日志）的片段有 ≥99% 通过，同时合成扰动 + 采集到的不可行验证窗口有 ≥95%
被拒绝。在论文中以精确率/召回率曲线的形式报告最终工作点 —— 不含糊。

### 2.3 采样层面的集成（算法上具有新颖性的部分）

由于 D_feas 是噪声条件化的（在所有 k 上训练），它可以在采样器的每一个 DDIM
步骤上对**中间的 x̂₀ 预测**打分 —— 而不只是对最终输出打分：

- **早期否决/重采样：** 在 DDIM 步 k = {40, 20, 10} 处对 x̂₀ 打分；如果批次中
  最好的样本仍低于 τ，则对该轨迹分支重新加噪并重新采样（最多 R 次重试）——
  拒绝采样被移到采样器*内部*，比整段重新采样更便宜。
- **可行性引导：** 在反向更新中加入判别器引导项（Kim et al., ICML 2023，
  图像领域）：
  `ε̂' = ε̂ − w(k) · √(1−ᾱ_k) · ∇_{A^k} log D_feas(A^k, s, k)`
  将样本引导向可行流形，而不是简单地拦截它们。w(k) 退火至 k 较小时为 0（早期
  引导，后期定型）。据我们所知，判别器引导此前尚未被用于机器人扩散策略中的
  *具身可行性*——这是本提案中最纯粹的算法新颖点，并且直接复用了框架自身的
  组件（优雅之处：X-Diffusion 本来就要训练这个判别器；我们只是让它在部署时
  也发挥作用）。

实现：`models/xdiffusion/shield.py`（打分 + 引导钩子）、
`deploy_xdiffusion_ur10e.py` worker（约 +60 行）、标志 `--shield-run-dir
--shield-mode --shield-thr --shield-guidance-w`。先做离线回放验证：将记录的
08-05 OOD 会话（任务中途人手撤离 → 甩鞭式抖动）重新跑一遍安全阀，统计否决/
抖动突发次数，然后再上机器人测试。

### 2.4 诚实的局限性（写进论文里）

- 安全阀评判的是*片段*，而非长时程意图：一个看起来可行的片段仍可能在任务
  层面是错的。它是动力学/可供性过滤器，不是任务监控器 —— 应与运行时任务
  监控器（Sentinel/STAC 风格）组合使用，而不是与之竞争。
- 否决 = 可用性损失；应与安全收益一并报告干预率。
- 引导可能会把样本推出策略的数据流形；需要限定 w(k) 的范围并做消融实验。

---

## 3. 设计 C —— 判别器作为数据集标注器（问题 2）

对任意示范数据池做离线遍历：滑动 8 帧窗口，在低 k 网格上对三个头都打分 →
得到逐帧的画像 `identity(t), kin_feas(t), afford_feas(t)`，存为 h5 键。
成本低（每个窗口一次前向）。四种训练用途，按「产出/投入比」排序：

1. **软准入（替代 Eq. 4 的硬门控）。** X-Diffusion 中，人类样本被纳入监督的
   条件是 `k ≥ k*`（二值蒙特卡洛门控）。将该指示函数替换为
   `compute_xdiffusion_loss_masks()` 内部的连续权重
   `w = P_feas(A, s, k)^γ` —— 一行代码的改动，可直接与论文做消融对比（γ 扫描；
   γ→∞ 时退化回硬门控）。消除了阈值=0.5 的随意性（Eq. 3）以及我们实测到的
   准入率不稳定问题。
2. **分段掩码联合训练（HRC 风格的扩展）。** 对于含有不可行*片段*的示范
   （afford_feas(t) 持续偏低）：在这些片段上屏蔽动作损失，但*保留*这些帧
   作为观测/条件上下文。策略由此学会「在无法模仿的伙伴阶段中等待/预判」——
   这是我们交接设计的一般化（伙伴的手在设计上本就只作为条件输入）。与论文的
   FILTERED 基线（整段丢弃 50% 的示范）以及 X-Diffusion（仅在高噪声下整体
   准入窗口）相比：我们保留每一帧，只是各归其位。这正是让 §4 中*混合*
   可行/不可行数据集变得可训练的关键。
3. **可行性条件生成（进阶方向）。** 将 `f` 作为带 dropout 的额外条件输入；
   部署时将 f 钳制为 1 → 类似分类器无关引导（classifier-free guidance）
   风格的「生成可行模式」。与设计 B 的引导机制存在交互；论文中二选一，
   做消融对比。
4. **数据质量看板/课程设计：** 按画像对示范排序，发现采集中的错误（我们此前
   靠人工发现过录制卡顿造成的瞬移；判别器可以自动完成这类取证工作）。

---

## 4. 设计 D —— 可行/不可行双人类数据集（数据规格）

在现有平台上采集两份匹配的数据集（相同摄像头/标定/HaMeR 流程，各约 1-2 小时），
且是*伙伴条件化*的（可行性判定依赖交互情境，而不仅仅是动作本身）：

| | 可行数据集 | 不可行数据集 |
|---|---|---|
| 手 1（动作，重定向） | 拾取/放置/推动/交接碗 | 手内转笔；拧盖并反复调整抓握；夹起筷子；将卡片滑到桌边并翻转；折叠布料；单手持物时按小按钮 |
| 手 2（条件） | 张开手掌伸手（「递给我」） | 指向物体 / 旋转手势（「打开它」「转一下」） |
| 重定向后的信号特征 | 干净的 EEF 轨迹 | 位置在限位内，但有夹爪闪烁 + 欧拉角噪声 + 微抖动 |

从试点实验中得到的设计原则：
- 选择那些重定向后 EEF 轨迹**在限位内但语义上不可执行**的不可行任务 ——
  这才是只有*学习型*判别器能捕捉到的类别（解析式逆运动学检查已经能免费捕捉
  剩下的类别，不要把采集精力浪费在超出限位的动作上）。
- 两个数据集使用相同的物体、桌面区域、光照、被试 —— 否则判别器学到的是
  场景而不是可行性（平衡性检查：训练一个场景分类探针，要求其准确率处于
  随机水平）。
- 抓取通道的真值标注复用现有的 SAM3 碗接触构建器；对于非碗类物体，为每个
  物体新增一条概念轨迹（每个物体一行代码）。

**这套数据集能实现的机器人演示**（论文的点睛之笔）：伙伴发出可行请求 →
机器人执行交接；伙伴发出不可行请求（指向笔、做旋转手势）→ *同一个*策略的
输出被判定为不可行 → 安全阀保持姿态/拒绝执行，可选配 LED/UI 信号提示。
「一个知道自己做不到什么的机器人」——协作式安全，在真实硬件上运行，使用的是
框架自带的判别器。

---

## 5. 问题 3 —— 现有成果的贡献地图

按（新颖性 × 已有证据 ÷ 剩余工作量）排序为 S/A/B 等级：

- **S1｜身份 ≠ 可行性 + 可行性判别器三件套**（本提案）：诊断性试点实验已
  完成；判别器 + 安全阀 + 标注器设计已如上；还需采集数据 + 重新训练 + 机器人
  A/B 测试。这是唯一带有*算法性*主张的部分（可行性引导采样）。
- **S2｜一份人-人数据集 → 双向交互方向 → 统一的模式选择策略**（已完成，
  08-05 已在机器人上验证）：角色重新分配（交换哪只手是「机器人」）、侧别
  比特条件化、入口边锁定、决策点处的过渡桥接、分支矩阵评估。MT-π/X-Diffusion
  这条研究线中，没有人从 HH（人-人）数据中训练*协作*（伙伴条件化）策略，
  更别提从*单次*录制中训练出双向能力。这是本论文数据效率方面的核心故事。
- **S3｜零样本：0 条机器人示范**（08-05 训练完成，离线门控已通过，机器人
  测试待定）：如果机器人测试哪怕部分成功，S2 就能升级为「仅靠 HH 数据就足够」，
  机器人示范数量（0 / 5 / 12）就能成为论文的横轴。
- **A1｜反事实条件 dropout**（已完成 + 已测量）：条件在单个 episode 内近似
  恒定 → 策略会忽略它们（我们的 v1 版本：双臂 72% 的误释放率）；作用域受控的
  哨兵 dropout 人工制造了反事实场景（→ 误释放率降至 0%，反应式等待行为已在
  机器人上验证）。这是条件化模仿学习的一般性原则，已有清晰的消融实验。作为
  方法部分并入 S2。
- **A2｜软准入替代硬 k\* 门控**（设计 C-1）：改动很小，可直接在公开任务 +
  我们自己的任务上与论文的 Eq. 4 做消融对比。
- **B｜系统性发现**（时间戳均匀重采样的取证分析、流水线化部署、观测时程/
  速度盲区、CRISP 阻尼哨兵）：诚实的 workshop/附录材料 —— 增强可复现性，
  但不是核心卖点。

**推荐的论文方向**：《*从人-人协作中进行可行性感知的跨具身学习*》
= S2（数据故事 + 机器人验证的交接）+ S1（判别器：诊断 → 修复 → 三种用途）+
A1/A2（方法改进）+ S3（机器人示范数量轴），在交接任务套件 + §4 的可行/不可行
任务套件上评估。

---

## 6. 实验矩阵（论文可用）

**离线表格**：（i）§0 的剖析表（身份 vs 可行性 AUROC），对比原始分类器与
D_feas（预期动力学扰动族上从 0.5→0.9+，同时保持 0.98 的身份区分度）；
（ii）准入权重消融（硬 k* / 软 γ / 强制可见）在交接任务 + 论文风格任务上的
对比；（iii）标注器精确率与解析式逆运动学筛查、以及与人工标注在 200 个窗口
上的对比。

**机器人 A/B 测试**（成功率 + 安全事件 + 干预率，每个单元格 ≥10 次试验）：
1. 统一交接：安全阀关闭 vs SELECT vs SELECT+VETO —— 包括可复现地引发
   40-54 mm 抖动突发的脚本化 OOD 触发（任务中途撤手）。
2. 可行/不可行请求套件（§4）：朴素联合训练策略 vs 分段掩码策略 vs 分段掩码
   + 安全阀。指标：可行请求成功率、不可行请求的拒绝率、可行请求上的误拒绝率。
3. 零样本 vs 5 条示范 vs 12 条示范的统一策略（S3 轴）。

**基线**：X-Diffusion 原版（硬门控，无安全阀）、FILTERED（解析式筛查丢弃）、
朴素联合训练、纯机器人数据、+ 运行时监控基线（STAC 风格的一致性检查，用于
安全阀对比）。

---

## 7. 风险 / 待解决问题

- **判别器走捷径的风险**：合成负例容易被过拟合（学到的是「检测扰动算子」，
  而不是「检测不可行性」）。缓解措施：使用宽幅度范围、在评估中保留未见过的
  扰动族（训练时不含瞬移，测试时专门测瞬移）、以采集到的可供性数据集作为
  真正的测试。
- **安全阀 vs 随机策略**：扩散模型的输出本质上是多模态的；SELECT 不能让
  多样性坍缩（保持 τ_soft 宽松；在前后测量采样多样性）。
- **审稿人可能问：「为什么不用关节限位检查？」**——答案在 §1 来源 2 + §4：
  我们*确实*把解析式检查用作运动学类别的免费标签；而可供性类别（在限位内
  但不可执行）对解析式检查是不可见的；学习型、噪声条件化的判别器统一了
  这两者，*并且*可以接入训练准入环节，这是任何解析式检查都做不到的。
- **审稿人可能问：「安全阀 ≈ OOD 检测？」**——与运行时监控器（一致性/嵌入
  OOD 检测）相比的立场：D_feas 是*按构造沿命名的物理轴有监督训练*的、
  噪声索引化的（因此能在采样器中途工作），并且同时充当训练门控 ——
  同一个对象同时服务于训练时和运行时，这种统一性才是我们的主张。
- **判别器自身的 OOD 可靠性**（我们 07-17 DANN 的第一手教训：判别器可以在
  其理想指标上表现完美，而特征本身是病态的）：报告判别器在分布偏移下的
  校准情况；保留扰动族的评估（§7 第一条）就是这方面的保障。

## 7.5 相关工作与新颖性判定（文献调研，2026-08-06）

X-Diffusion 本身：**arXiv 2511.04671, ICRA 2026**（康奈尔大学）；已确认其
分类器仅在训练时使用（「我们只有在 k ≥ k* 时才将人类动作纳入损失」）；
其未来工作方向是扩展到未经筛选的视频数据（与我们的方向正交）。

**设计 B（采集不可行对照数据 + 标注）—— 新颖性最强。** 目前没有工作通过
采集*配对的可行/不可行*人类数据来将具身分类器升级为可行性判别器。最接近
的机制：DWBC（ICML 22, 2207.10050）—— 判别器加权的行为克隆，但是在*单一*
具身内区分专家/次优；DemInf（2502.08623）/ Re-Mix（CoRL 24）—— 质量/
混合加权，而非可行性判定。整条人→机器人研究线（Phantom CoRL 25 ——
*明确将采集限制为捏取抓握，因为机器人是平行夹爪*；Mirage、EgoMimic、
HumanPlus、DexCap、MT-π、Point Policy）都是通过**采集协议**来处理不可行性，
即：回避它 —— 没有一个工作去检测或建模它。这是论文中可以直接展开论证对比
的地方。

**设计 A（部署时安全阀 / 可行性引导）—— 新颖，但需谨慎定位。** 判别器引导
（Kim, ICML 23, 2211.17091）目前还没有在机器人策略中的应用。最接近的工作：
SafeDiffuser（ICLR 25, 2306.00148）—— 在去噪过程内部加约束，但用的是手工
设计的 CBF，而非学习得到的约束；Learned Viability Filters（2502.19564）——
best-of-N + 运行时过滤器，但是用于角色动画且用的是 RL 价值函数过滤器。
运行时监控器（Sentinel/STAC CoRL 24 2410.04640、FAIL-Detect RSS 25
2503.08558、FIPER NeurIPS 25）只会报警，从不做「否决并重新采样」，也不携带
具身语义 —— 可作为安全阀对比的基线。我们的角度：这个过滤器是框架*自身*
冻结后的副产品，天然噪声条件化，因此可以原生地对每个 DDIM 步骤打分。

**设计 C（连续的噪声索引化准入）—— 消融级别，不能独立成篇。** 介于
X-Diffusion 的硬 k* 门控和 Ambient Diffusion Policy（2606.12365）的
逐数据集时间步分段之间。应作为设计 A/B 的组成部分来呈现，而非独立卖点。

## 8. 两周执行清单

1. 编写 `corruptions.py` 并用合成负例重新训练判别器；重跑 §0 的表格
   （预期动力学扰动族的 AUROC → 0.9+）。[1-2 天]
2. 逆运动学筛查自动标注器；标注器 vs 解析式方法的对比表。[1 天]
3. 在部署 worker 中集成安全阀 + 离线回放 08-05 的 OOD 日志；然后进行机器人
   A/B 测试 1。[2-3 天]
4. 采集 §4 的可行 + 不可行数据集（带伙伴条件化）；重新训练可供性头；
   进行机器人 A/B 测试 2。[3-4 天]
5. 软准入消融训练实验。[1 天，可并行]
6. 零样本机器人测试（已排队）→ 得到 S3 单元格数据。[0.5 天]
