# 可行性评判器（Feasibility Critic）—— 详细实现计划（2026-08-06）

`RESEARCH_PROPOSAL_FEASIBILITY.md` 的配套文档。本文档把设计 B（08-06 文献
调研判定 novelty 最强的方向：*配对采集可行/不可行双人数据，把具身身份分类器
升级为可行性评判器*）落地为可执行的工程计划：机制、数据规格、代码改动、训练
配方，以及——详细展开——用来确立"评判器是否真的能分辨可行与不可行动作"的
评测协议。

---

## 1. 机制：评判器如何工作

### 1.1 输入、架构与噪声轴

评判器完全保留现有分类器（`models/xdiffusion/hr_classifier.py`）的输入输出
契约，因此所有消费方（训练录取、标注器、部署 shield）都可以即插即用：

- **动作块** `A ∈ R^{8×7}`：未来 8 步的 `[末端位置(3), 末端欧拉角(3), 夹爪(1)]`，
  用评判器自己的数据集统计量做 min-max 归一化到 [−1,1]（存在其 run 目录里；
  消费方**必须**加载这份统计量而不是策略的——静默的统计量不匹配是头号可预见
  的集成 bug，要加断言检查）。
- **状态** `s ∈ R^7`：观测时刻的本体行（同样 7 维）。
- **扩散步** `k ∈ [0, 101)`：前向加噪 `A^k = √ᾱ_k·A + √(1−ᾱ_k)·ε`，与策略
  同一套 squaredcos_cap_v2 schedule；`k` 通过正弦时间步嵌入（256 维）进入
  网络。噪声同时施加于 `A` 和 `s`（与 `unified_forward` 一致）。
- **主干**：`HalfUnet1D` —— 时间 Conv1d 栈（kernel 5，down_dims
  [256,512,1024]），以（时间步嵌入 ⊕ 展平状态）做 FiLM 条件化。
- **输出头**（改动处）：时间维全局池化 → `Linear(1024, 3)` = 三个 sigmoid 头
  `[identity(身份), kin_feas(运动学可行), afford_feas(affordance 可行)]`，
  取代原来单一的身份 logit。

为什么时间卷积是正确的归纳偏置：有限差分本质上就是线性卷积，所以 kernel-5
的卷积栈天然能在多个时间尺度上算出逐步速度、加速度、jerk、符号交替和抓取
跳变模式——这恰好是可行动作与不可行动作在统计上产生差异的地方。08-06 的
先导实验证明，现成分类器的失败是**缺监督**问题，而不是容量问题：同一个主干
在抓取翻振/欧拉旋转上已经能打出 0.67/0.77 的 AUROC——这是它两个训练类别
恰好存在差异的仅有两个腐蚀轴。给它沿速度/连续性轴真正分开的类别，梯度下降
会立刻找到这些特征（它们是间隔最大的特征）。

为什么要保留噪声轴（三条理由，每条都是承重的）：

1. **与部署对齐**：DDIM 采样过程中，shield 在实际噪声级 `k`（或 k≈0 时的
   x̂₀ 估计）上给中间状态打分，零分布偏移——因为评判器训练时用的正是这样
   被加噪的动作块。
2. **k\* 机制可以推广**：X-Diffusion 的最小不可区分步（Eq. 3）变成
   **最小可行步**
   `k*_feas(A) = min{ k : P_feas(A^k, s, k) ≥ 0.5 }`——"这条人类行为要抽象
   到什么程度才对机器人可用"——现在是针对真实可行性标签衡量，而不是把
   具身身份当代理。
3. **正则化**：在高 k 处任务被证明是不可能的（分布合并——Ambient Diffusion
   的前提），这会把网络推向鲁棒的低 k 特征而不是脆弱的捷径。

### 1.2 训练类别（三个负样本来源，一个正样本池）

| 头 | 正样本 (y=1) | 负样本 (y=0) | 成本 |
|---|---|---|---|
| `identity` | 机器人窗口 | 人类窗口 | 免费（现成）|
| `kin_feas` | 干净机器人 + 通过 IK 筛的人类 | (a) 对机器人/可行人类窗口的合成腐蚀；(b) 未通过 IK 筛的人类窗口 | 免费 |
| `afford_feas` | 机器人 + **可行任务**人类 | **不可行任务**人类（新采集，见 §2）| 约 2 个半天 |

逐头掩码 BCE：`L = Σ_h λ_h · BCE(head_h, y_h) · defined_h`——每个样本只
携带对它有意义的头的标签（一个合成腐蚀样本有 `kin=0` 但 `afford` 未定义
→ 掩掉）。λ 初始全为 1.0。逐头平衡采样复用现有 `balanced_sampling_weights`
机制（推广到逐头池）。时间步 `k ~ U[0,101)` 与原版一致（保持可比性；低 k
重要性采样作为消融项，不是默认设置）。

合成腐蚀族（在 `dataset_utils/corruptions.py`，在加载器里对 RAW 空间的
动作块施加，*在归一化之前*，每个样本以概率 `p_corrupt` 触发，幅度每次
抽取随机化）：

| 腐蚀族 | 算子 | 幅度范围 |
|---|---|---|
| 加速 | `p₀ + m·(p−p₀)` | m ∈ [1.5, 6] |
| 传送 | 随机位置的阶跃跳变 | [2, 10] cm |
| 抖动 | iid 位置噪声 | [5, 25] mm |
| 甩鞭 (whipsaw) | 交替 ±y 偏移 | [15, 60] mm |
| 欧拉乱转 | yaw 斜坡 | [0.1, 0.5] rad/step |
| 抓取翻振 | 0/1 切换 | 每窗 2–7 次跳变 |
| 平滑违反 | 丢帧重采样（混叠）| 1–2 帧 |

幅度范围的取舍依据：下界刚好高于我们真实部署的运行包络（策略已验证成功
的会话不能落在负样本支撑集内）；上界是先导实验中今天判为随机水平的幅度。
评测阶段的幅度扫掠（§4, E1c）会定位学到的边界。

`afford_feas` 标签噪声：标签继承自任务片段，所以一个不可行 episode 内的
静止/搬运子窗口局部上是可行的——弱标签。v1 缓解措施：对 afford 负样本
做标签平滑 0.9/0.1 + 报告段级指标（对边界噪声鲁棒）并与窗口级并列。如果
窗口级指标不理想，升级路径是对 episode 的窗口做注意力池化的多示例学习
（Ilse et al., ICML 2018）——即使实例标签不干净，包（bag）标签也是干净的。

### 1.3 输出与校准

原始的平衡 BCE sigmoid 不是校准过的概率。事后**温度缩放**（Guo et al.,
ICML 2017）在留出验证集上逐头拟合，在 k 网格 {0,5,10,20,40} 上拟合出
`T_h(k)` 表（网格点间线性插值），存在 run 目录里；报告前后 ECE。消费方：

- **Shield 分数**：`f = min(kin_feas, afford_feas)`，在 k=0 处（保守取
  AND；identity 头故意**排除**——先导实验证明 identity 会拒掉良性的
  human 风格输出）。平均 2–4 次噪声抽样（在 k=0 处 ᾱ≈1，抽样方差很小——
  测量并把抽样次数压到最小以控制延迟）。
- **标注器画像**：滑窗扫过一条演示，逐帧
  `feas(t) = 覆盖该帧的所有窗口的均值`，存为 h5 键
  `crit_kin`、`crit_afford`，外加每窗的 `k*_feas`。
- **软录取权重**：`w = P_feas(A^k, s, k)^γ`，替换
  `compute_xdiffusion_loss_masks()` 里的硬门 `1{k ≥ k*}`。

### 1.4 结构是否足够？（08-06 讨论后补充）

关于"7 自由度输入的 HalfUnet 能否判断可行性"的三层诚实结论：

**第一层——运动学/动力学违规：现有架构已经足够。**
速度、传送、抖动、甩鞭、乱转都是逐步差分统计量；时间 Conv1d 天然计算
多阶差分，先导实验已经证明只要监督需要，这个主干就能提取这类特征（在
零可行性训练的情况下，翻振 0.67 / 乱转 0.77）。先导实验的随机水平结果
是缺监督问题，不是容量问题。

**第二层——低成本的架构升级（三项都采纳）。**
- **双池化**：把全局平均池化换成 concat[平均池化, 最大池化] →
  `Linear(2048, 3)`。单步传送只在一个时间位置产生一个大的时间特征，
  平均池化会把它稀释 1/8；最大池化保留峰值违规。一行改动。
- **把评判器窗口与策略的 pred_horizon 解耦**（最重要的一项）：8 步
  @15Hz = 0.53 秒，但拧盖换抓循环或转笔周期跑 1–2 秒——在单个 8 步
  窗口内，一个换抓片段（"手腕转动的同时松开"）局部看起来就像一次
  可行的正常释放。改为在 **24 步窗口**（1.6 秒）上训练评判器。部署
  时打分对象是 `[最近 16 步已执行 ‖ 8 步预测]`——已执行历史在部署
  循环中是免费的，而这个拼接把**跨 chunk 的不连续**（跨 chunk 边界
  的甩鞭——任何逐 chunk 打分器都看不见）纳入了感受野。策略本身的
  horizon 不受影响；只是评判器的输入长度不同。用于训练录取时，对
  以策略采样窗口结尾的 24 步窗口打分。
- **3 成员集成**（3 个随机种子 × 约 30 分钟）：分数分歧 = 认知不
  确定性，让 shield 区分"确信不可行"和"从未见过类似情况"（对应
  不同的回退策略；也是对判别器可靠性/DANN 担忧的具体工程回答）。

**第三层——输入表征的极限（任何基于 7 自由度的架构都无法修复）。**
Affordance 不可行性按"其签名是否在重定向到腕部+捏合轨迹之后仍然存活"
分裂：
- *轨迹可见*：转笔（位置–姿态解耦）、拧盖换抓（"握持中"的抓取振荡）、
  叠布（微换抓翻振）——可从 7 自由度学到。
- *轨迹不可见*：筷子夹取（腕部+捏合轨迹 ≈ 一次完全可行的捏取+提起；
  不可行性存在于工具介导的手指接触中）和多键和弦（腕部轨迹 ≈ 可行的
  悬停+轻戳；张开手指的同时性消失了）。从重定向后的轨迹上信息论意义
  上不可检测——**因为重定向这个操作本身就销毁了证据**。

修复方案 = **双输入评判器**：共享的时间主干处理 7 自由度核心 + 一个
可选的**手指特征分支**（来自重定向之前就有的 HaMeR 关键点：相对腕部
坐标系的 5 个指尖位置，每帧 15 维，用并行卷积栈编码，在输出头之前融合），
以分支 dropout 方式训练，使模型带/不带手指特征都能工作。分工是：
**标注器/训练录取门带手指跑**（人类数据永远带有手部关键点，筷子/和弦
因此变得可见）；**部署 shield 不带手指跑**（策略输出没有手指数据——
而且 shield 相关的失败恰好是动力学/OOD 类，本来就是 7 自由度可见的）。
这同时也把论文本该分开的概念理清了：

> 不可行 = **可执行性失败**（机械臂跑不了这条轨迹——7 自由度评判器）
> ∨ **有效性失败**（机械臂能跑，但重定向丢失了行为的本质，跑了也无法
> 完成任务——手指级评判器，本质上是一个*可重定向性*评判器）。

筷子/和弦属于有效性失败。为 E2 登记的预测：7 自由度评判器恰好在这两个
任务上失败，而手指分支评判器能把它们救回来——这张逐任务可见性表就是
在**量化哪些不可行性在重定向中存活**，这是之前没有任何工作量化过的量。

### 1.5 数据层 vs 部署层——以及策略的输出能否骗过评判器？

评判器在**两个层面**都起作用，按失败来源有严格的分工：

| 失败来源 | 例子 | 在哪拦截 | 用什么 |
|---|---|---|---|
| 策略会从演示中**学到**精细的不可行执行 | 转笔的细节循环 | 数据层（录取门：低 k 监督被挡）| 7 自由度头 |
| 重定向已销毁证据的不可行 | 筷子、和弦 | 只能在数据层（手指分支标注器掩码片段）| 手指分支 |
| 运行时 OOD 生成，从未出现在任何数据中 | 08-05 手部时机异常时的甩鞭爆发 | 部署 shield | kin 头 |
| 漏过录取门的残余不可行模态 | 被衰减的翻振 | 部署 shield（+ 下述刷新机制）| kin/afford 头 |
| 动力学没毛病但任务错误的输出 | 筷子情境下"看似可行的捏取" | 部署层从 7 自由度无法捕捉——必须在数据层预防 | （分工）|

三个支撑事实：
1. **完美的数据清洗不会让 shield 变得多余**：我们实测的甩鞭失败来自
   一个**完全用可行数据训练**的策略——这是一个 OOD 条件下的生成失败，
   任何训练数据过滤器都无法阻止它。
2. **扩散策略保留模态**（这是它相对 MSE-BC 的定义性特性）：在翻振/
   解耦签名上训练的策略会在采样中复现这些签名，所以轨迹可见的不可行
   模态在部署时对评判器依然可见。但要注意：少步 DDIM + 小数据规模
   可能会**衰减**高频签名 → **下面新增的 E4-0** 在策略**生成**的
   动作块上（而不仅仅是数据块上）评测评判器；如果分离度下降，做
   一轮**对抗式刷新**（把生成的负样本加进评判器训练，重训一次）就
   能弥合差距。
3. **结构上不存在军备竞赛**：策略不会从评判器接收任何梯度（录取只是
   挑选谁来做监督，不是把策略优化成让评判器打高分；shield 只在推理
   期起作用），所以没有任何力量把策略推向"骗过评判器"的输出。
   例外：GUIDANCE（引导）变体会主动向评判器高分区攀爬，可能利用它
   的缺陷——对引导权重设界，并用集成一致性来门控。

v1 接受的已知遗留 gap：仅用末端位姿的状态隐藏了关节构型（奇异点附近，
末端速度限位是构型相关的：同一个末端位姿，肘部朝上可能没问题，肘部
朝下可能接近奇异）。我们的桌面工作空间始终停留在单一 IK 分支、远离
奇异点，所以这个近似在这里成立；V2 = 把 q∈R^6 拼进条件（状态从 7→13）
以获得通用性。图像/SAM3 掩码（物体上下文）留作 V2+ 扩展，通过仓库中
故意留空的 `ImageHumanRobotClassifier` 接入。

---

## 2. 配对的可行/不可行数据采集（数据规格）

设备：不变（两台标定过的 Orbbec、HaMeR + SAM3、side-locked 双手流水线，
`--resample-hz 15` 时间戳均匀导出）。

**可行集（约 40 集）**：手 1 对桌面物体做拾取/放置/推动/交接；手 2 给出
*请求*手势（摊掌伸手）或不出现。大部分已被现有 29-07 数据池覆盖
（136 集）——只需为物体多样性补录。

**不可行集（约 40–60 集）**——选取规则：*重定向后是限位内的末端运动，
但 2F-85 平行爪无法执行*（限位外的动作用解析筛查免费就能抓到，不值得
花采集时间）：

| # | 任务 | 7 自由度轨迹中的签名 | 对 7 自由度评判器可见？ |
|---|---|---|---|
| 1 | 手中转笔 | 手腕准静止，欧拉角剧烈变化（位置–姿态解耦）| 是 |
| 2 | 拧盖换抓循环 | "握持中"的抓取振荡 + 旋转（需要 24 步窗口）| 是 |
| 3 | 用手指叠布 | 微换抓翻振、聚拢微动作 | 是 |
| 4 | 卡片滑边+翻面 | 净手腕位移接近零 + 突然的翻转欧拉角 | 部分 |
| 5 | 筷子夹取 | 轨迹 ≈ 一次可行的捏取+提起 | **否——需要手指分支** |
| 6 | 多键和弦按压（2-3 键，手指张开）| 轨迹 ≈ 可行的悬停+轻戳 | **否——需要手指分支** |

任务 5–6 被刻意保留：它们是 §1.4 双输入消融的注册预测格（7 自由度
评判器理应在此失败；手指分支评判器理应能把它们救回来）。排除项：
空爪闭合戳单个大按钮是**可行**的——不采集；"持物时按键"属于任务
语义上的不可行——采集但单独分析，不计入头条指标。

搭档条件化：每条不可行 episode 的手 2 都做出匹配的请求手势（指笔 /
转拳"打开它" / 伸出布料），所以可行性是**由交互条件决定的**——部署
时同一个策略在摊掌条件下产生通过 shield 的动作块，在指笔条件下产生
被 shield 否决的动作块。这组配对给出了机器人 demo 所需的因果对照。

卫生规则（每条都是因为之前有捷径靠它才被识破的）：
- 两个集合用相同的被试、相同的桌面区域，物体在两个集合中都**可见于
  桌面上**；相同的拍摄光照；录制顺序做反平衡。
- 不可行 episode 的抓取通道：保留原始捏合阈值信号（不做物体接触清洗）
  ——凌乱的抓取本身就是签名；把这种不对称记录在案。
- 两集合之间的起始位姿匹配（E2 中的起始位姿探针必须落在随机水平）。
- 在新导出器 `export_xdiffusion_h5_feas.py`（双人导出器的轻量派生，
  逐任务 SAM3 概念提示词："pen"、"bottle"、"chopsticks"、"card"、
  "cloth"、"keyboard"）中写入逐 episode 的 `feas_label` 属性和逐帧
  `feas_mask`（首尾过渡帧标记为可行-静止）。

数据量：40 集 × 约 35 秒 × 15 Hz ≈ 21k 帧 ≈ 每类 10k 窗口（步幅 2）——
对于一个已经在 5 万+窗口上训练的约 2000 万参数主干来说绰绰有余。

## 3. 代码改动（文件级）

1. `dataset_utils/corruptions.py`（新建，约 150 行）：纯函数
   `(chunk_raw, rng, magnitude) → chunk_raw`，腐蚀族注册表，
   `CorruptionConfig`（腐蚀族、p_corrupt、幅度范围、课程学习开关）。
   单元测试：每个腐蚀族只改动它对应的通道；幅度在给定种子下可复现。
2. `dataset_utils/h5_dataset.py`：在 `__getitem__` 中紧挨着
   `normalize_data` **之前**挂上腐蚀采样钩子；产出
   `(sample, head_labels, head_mask)`；逐头平衡采样器。
3. `models/xdiffusion/hr_classifier.py`：`FeasibilityCritic
   (HumanRobotClassifier)`，`Linear(1024, 3)`，`loss_multitask()`
   （掩码 BCE），向后兼容的单头 checkpoint 迁移（旧 `classifier.2`
   权重的键名重映射）。
4. `scripts/ik_screen.py`（新建）：v1 = 末端空间筛查（工作空间盒含
   桌面净空、逐步速度/加速度对照我们的部署限位、朝向可达性启发式）；
   v1.1 = UR10e 闭式 IK、关节分支连续性 + 关节速度限位 + 腕部/肘部
   奇异裕度。写出逐窗口 `ik_feas` h5 键（`kin_feas` 的标签来源，
   E1/E2 的基线）。
5. `scripts/train_critic.py`（扩展 `train_classifier.py`）：三池
   组装、逐头 wandb 指标、**验证集上每 epoch 的 AUROC@k∈{0,10,20,40}**
   ——复用分类器 run 目录里已有的 `noise_sweep` 工具。
6. `scripts/calibrate_critic.py`（新建，约 80 行）：逐头逐 k 网格点
   的温度拟合；写出 `calibration.json`；绘制可靠性图。
7. `scripts/critic_label_pass.py`（新建）：离线标注器——在任意 h5
   数据池上滑窗，写出 `crit_*` 键 + 逐演示画像 PNG。
8. 部署（`deploy_xdiffusion_ur10e.py` worker）：`--shield-run-dir
   --shield-mode {off,select,veto,both} --shield-thr --shield-draws`
   （见提案 §2）——消费评判器 run 目录（ckpt + 统计量 + 校准）。

---

## 4. 评测：如何确立评判器真的能分辨可行与不可行

设计原则：每个指标都必须区分**"学到了不可行性这个概念"**与
**"背下了负样本生成算子"**。四层，由便宜到昂贵。

### E1 —— 运动学头，合成真值

- **E1a，分布内**：干净机器人验证集 vs 每个训练过的腐蚀族，在
  k∈{0,5,10,20,40} 处的 AUROC。达标线：k≤10 时 ≥0.95（对照今天的
  0.49–0.55——这张前后对比表是论文的第一个结果）。
- **E1b，留一族出（LOFO）**——泛化测试：整族剔除后重训，在该族上
  测试。完整 7×7 矩阵（训练集 × 测试族）。只有当网络学到了共享统计
  量（速度/加速度/连续性）而非逐算子签名时，迁移才会发生。达标线：
  留出族平均 AUROC ≥0.85。这种评测风格的先例：腐蚀鲁棒性基准
  （Hendrycks & Dietterich, ICLR 2019）——同样的逻辑，用在我们的
  领域。
- **E1c，幅度扫掠/边界定位**：AUROC 和 P_feas 随腐蚀幅度变化（速度
  1.1×→6× 等）。叠加解析极限（UR10e 速度/加速度包络）：评判器的
  50% 交叉点是否落在物理边界上？如果是——这就是招牌图：**学到的
  边界与物理边界重合，而它从未被告知过限位**。
- **E1d，静止对照**：冻结的动作块必须打分为可行（它们确实可行）。
  防止"低活动量=不可行"的坍缩。
- 统计方法：窗口按 episode 聚类 → 聚类自助法置信区间（1000 次重
  采样，episode 级），每格 n ≥ 300 窗口。

### E2 —— affordance 头，采集得到的真值（novel 主张的测试）

划分方式，严格按 episode 互斥，三层嵌套的泛化级别：
1. **留出 episode**（同任务）——健全性检查，预期高分；
2. **留一任务出（LOTO）**：用 {转笔、拧盖换抓、叠布、和弦} 训练
   affordance 头，在 {筷子} 上测试；轮换。检验"爪子不可行"是否是
   跨灵巧行为的单一可学概念。达标线：窗口级 ≥0.80，段级（episode
   片段内均值池化）≥0.90。
3. **留出被试**（若被试数 ≥2）：人不变性。

混淆对照（每一项都排除一个特定的竞争性解释）：
- **起始位姿探针**：仅用 `s` 做逻辑回归 → 必须 ≈0.5 AUROC（否则两
  个集合是按"在哪"而不是"是什么"可分的）。
- **仅运动窗 AUROC**：把两类中的静止窗口都排除 → 判别不是"静止
  检测"。
- **速度配对 AUROC**：按平均位移 |Δp| 分箱，箱内比较（倾向得分匹配）
  → 判别不是"灵巧动作只是移动速度不同"。达标线：配对后仍 ≥0.75。
- **人工一致性**：200 个随机窗口，2 名标注者观看源视频（"2F-85 能否
  执行这段片段重定向后的运动，并达成其目的？"）；报告评判器–人类
  的 Cohen's κ 与人类–人类的 κ 对比。为这个构念提供依据；预期
  κ_人人 本身就 < 1（边界本来就是模糊的——如实报告，不要藏起来）。

评判器必须打赢的基线（审稿人一定会要求全部四个）：
1. **现成的身份分类器**（= 我们的先导数据：动力学 ~0.5，翻振/乱转
   0.67/0.77）——"改进前"。
2. **解析 IK 筛查**——限位外满分，affordance 上 ~0.5（因构造使然）
   ——说明为什么必须学习。
3. **手工特征 + 逻辑回归**：{平均/最大 |Δp|、jerk 能量、抓取跳变
   计数、欧拉谱熵、位置–姿态相关性} → 如果 5 个特征能追平深度评判器，
   这个主张就弱了；预期评判器在 LOFO/LOTO 迁移和速度配对格上专门
   胜出。
4. **无监督 OOD 检测器**（仅在可行数据上拟合的主干特征空间 kNN
   距离，Sun et al. ICML 2022）：有监督对比是否胜过纯 OOD？预期：
   OOD 能部分标出 affordance 负样本，但也会误标新颖但可行的动作
   （精度差）——这个消融正是"为什么必须采集负样本"的理由。

外加**双输入消融**（§1.4 登记的预测）：7 自由度评判器 vs 手指分支
评判器的逐任务 AUROC。预期模式：任务 1–3 两者都高；任务 5–6 对
仅 7 自由度版本 ≈ 随机水平，手指分支能救回来。这张表*量化了哪些
不可行性在重定向中存活*——这是之前没有任何人机→机器人工作报告过
的量。

### E3 —— 在已知缺陷的数据上验证标注器（免费，我们已经拥有）

把评判器跑在去噪前的原始 HaMeR 轨迹（track1_raw 备份）和 07-29
重采样前的导出数据上。我们从自己的取证工作中拥有两类缺陷的逐帧
真值（`filter_hand_tracks.py` 的 Hampel 尖峰列表；时间基准审计中
的 27–32% 停顿-传送跳变）。指标：`crit_kin < τ` 相对这些列表的
精确率/召回率。达标线：R≥0.8 时 P≥0.8。这是零新增采集的真实世界
未整理数据验证——直接预演了 X-Diffusion 自己声明的未来工作（未
整理网络视频的整理）。

### E4 —— 部署 shield（对应提案 §2）

- **E4-0，生成动作块的分离度（"生成过程是否洗掉了签名？"测试）**：
  故意在混合数据池上（不可行数据处处放行）训练一个 naive 策略，在
  不可行情境条件下采样动作块，将其与可行情境样本以及底层数据窗口
  一起打分。度量签名在采样器中的衰减程度。如果 AUROC 相对数据层
  数字明显下降 → 做一轮对抗式刷新（把生成的负样本加进评判器训练）
  并重新测量。
- **离线重放**：08-05 记录的 OOD 会话（任务中途手部撤回 →
  每周期 40–54 mm 的甩鞭爆发）对比验证良好的会话：爆发周期的
  否决率 ≥90%，良好周期的误否决率 ≤1%（阈值来自校准后的分数）。
- **真机 A/B**：统一交接策略配合脚本化的 OOD 触发；shield 关闭/
  SELECT/VETO 三档；指标：爆发事件数、干预率、任务成功率保持情况。
- **真机 demo（论文的展示实验）**：搭档摊掌 → 执行交接；搭档指向
  笔 / 做旋转手势 → 策略输出被评判为不可行 → shield 锁定位姿（拒绝
  执行）。同一个策略、同一个 shield，因交互情境不同而产生相反的结果。

### k 剖面图（机制验证）

对每个类别，AUROC(k) 必须随 k→101 衰减到 0.5（分布被证明会合并）。
衰减速率按"离可行流形的距离"给各类排序：传送应该比轻微抖动在更高
的 k 处仍可分离。绘制"可行性持续谱"——每个类别的 AUROC vs k——
以及每个任务的逐样本 `k*_feas` 直方图。这用真实标签验证了论文
Fig. 3 的叙事，而不是靠肉眼判断的侧抓例子，也是此前不存在的定量
对象。

---

## 5. 参考文献

方法核心：
- Pace et al.，**X-Diffusion**，arXiv 2511.04671，ICRA 2026 —— 基础
  框架（Eq. 2 分类器、Eq. 3 k*、Eq. 4 门控损失）。
- Daras et al.，**Ambient Diffusion**，NeurIPS 2023，arXiv 2305.19256
  （+ **Ambient Diffusion Omni**，arXiv 2506.10038；+ **Ambient
  Diffusion Policy**，arXiv 2606.12365）—— 从受损数据中做噪声级门控
  学习；分类器的谱系源头。
- Kim et al.，**Refining Generative Process with Discriminator
  Guidance**，ICML 2023，arXiv 2211.17091 —— 我们移植的采样期引导项。
- Xu et al.，**DWBC**，ICML 2022，arXiv 2207.10050 —— 判别器加权的
  行为克隆（软权重机制的先例；但是同具身的数据质量问题，不是可行性）。
- Guo et al.，**On Calibration of Modern Neural Networks**，
  ICML 2017，arXiv 1706.04599 —— 温度缩放。
- Ilse et al.，**Attention-based Deep MIL**，ICML 2018，
  arXiv 1802.04712 —— 弱段级标签的升级路径。

评测方法学：
- Hendrycks & Dietterich，ICLR 2019，arXiv 1903.12261 —— 留出腐蚀
  评测的逻辑。
- Sun et al.，**kNN-OOD**，ICML 2022，arXiv 2204.06507 —— 无监督
  OOD 基线。

定位/基线：
- Agia et al.，**Sentinel/STAC**，CoRL 2024，arXiv 2410.04640 ——
  运行时一致性监控（部署层基线）。
- **FAIL-Detect**，RSS 2025，arXiv 2503.08558 —— 无需失败数据的
  失败检测（基线）。
- Xiao et al.，**SafeDiffuser**，ICLR 2025，arXiv 2306.00148 ——
  去噪过程内的手工 CBF（对照：我们的是学出来的）。
- **Learned Viability Filters**，arXiv 2502.19564 —— 最接近的
  best-of-N 运行时滤波器（动画领域，RL 价值语义）。
- **Phantom**，CoRL 2025，arXiv 2503.00779 —— 把采集限制在捏合抓取
  （采集协议规避不可行性的对照案例）；类似地还有 Mirage（RSS 2024，
  2402.19249）、EgoMimic（ICRA 2025，2410.24221）、MT-π（2501.06994）、
  Point Policy（2502.20391）。
- Hejna et al.，**DemInf**，arXiv 2502.08623 —— 数据质量筛选的对比
  对象。

## 6. 执行顺序与算力

| 步骤 | 内容 | 时长 | 依赖 |
|---|---|---|---|
| 1 | corruptions.py + 评判器多头 + 在现有数据池上训练运动学头 | 1–2 天 | — |
| 2 | E1a/b/c/d 全套（LOFO = 7 次重训，每次约 30 分钟）| 0.5 天 | 1 |
| 3 | ik_screen v1 + 标签 + 校准 | 1 天 | 1 |
| 4 | 不可行集采集 + 导出 + 质检 | 2 个半天 | —（并行）|
| 5 | affordance 头重训 + E2 全套 + 人工一致性（200 窗口）| 1.5 天 | 3,4 |
| 6 | E3 在原始轨迹上的取证验证 | 0.5 天 | 3 |
| 7 | shield 接入 worker + E4 离线重放 | 1 天 | 5 |
| 8 | 真机 A/B + demo | 1–2 天 | 7 |

在当前 GPU 上每次训练 ≤ 约 30 分钟（分类器量级的网络，现成的加载器）。
含机器人时间总计约 2 周，与提案 §8 一致。
